{
  "name": "oasis-p2p",
  "version": "0.10.0",
  "description": "A mesh network that shares files based on a community manifest file ",
  "main": "dist/index.js",
  "scripts": {
    "server": "npx ts-node index.ts",
    "test": "NODE_ENV=test TS_NODE_PROJECT=\"tsconfig.testing.json\" mocha --require ts-node/register 'test/**/*.ts' --timeout 25000",
    "build": "tsc"
  },
  "author": "apexmfer",
  "license": "MIT",
  "dependencies": {  
    "extensible-mongoose": "^0.25.2",
    "ts-node": "^9.1.1", 
    "web3-utils": "^1.7.1"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.1.0",
    "husky": "^7.0.4",
    "chai": "^4.3.4",
    "ethers": "^5.6.2",
    "mocha": "^9.2.2",
    "typescript": "^4.6.3"
  }
}
